public String seeColor(String str) {
  
  String res = "";
  
  for(int i=0;i<str.length();i++){
    
    res = res + str.substring(i,i+1);
    if(res.equals("red")){
      return "red";
    }if(res.equals("blue")){
      return "blue";
    }
    
  }  return "";
  
  
}
