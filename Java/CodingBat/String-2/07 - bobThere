public boolean bobThere(String str) {
  
  for(int i=0;i<str.length()-2;i++){
    
    int end = i+2;
    if(end>=str.length()){
      end=str.length();
    }
    
    if(str.substring(i,i+1).equals("b") && str.substring(end,end+1).equals("b")){
      return true;
    }
    
  }return false;
  
}
