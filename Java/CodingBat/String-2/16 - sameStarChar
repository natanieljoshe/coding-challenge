public boolean sameStarChar(String str) {
  
  boolean res=true;
  
  for(int i=1;i<str.length()-1;i++){
    if(str.charAt(i)=='*'){
      if(str.charAt(i-1)!=str.charAt(i+1) ){
        res=false;
      }
    }
  }
  return res;
}
