public String endUp(String str) {
  
  String res;
  if(str.length()<=3){
    return str.toUpperCase();
  }
  int cut = str.length()-3;
  String front = str.substring(0,cut);
  String back =  str.substring(cut,str.length()).toUpperCase();
  
  return front+back;
  
  
}
