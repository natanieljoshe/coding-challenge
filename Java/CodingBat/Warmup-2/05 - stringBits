public String stringBits(String str) {
  
  String res="";
  
  if(str.length()<2){
    return str;
  }
  else{
  for (int i=0;i<str.length();i=i+2){
    res=res+ str.substring(i,i+1);
  }return res;
  }
  
}
